export const ticketsSearchId = (state) => state.tickets.searchId
export const ticketsLength = (state) => state.tickets.length
export const ticketsArr = (state) => state.tickets.tickets
export const ticketsHasError = (state) => state.tickets.hasError
export const ticketsIsLoading = (state) => state.tickets.isLoading

export const sortType = (state) => state.sort.sort

export const transfers = (state) => state.transfer
